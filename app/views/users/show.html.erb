<% provide(:title, "User #{@user.name} ") %>

<h1><%= @user.name %> </h1>
<div class="row">
    <div class="col-6">
        <h2>Created events</h2>
        <%= render @user.events %>
    </div>
    <div class="col-6">
        <h2>Attending events</h2>
        <%= render @attended_events %>
    </div>
</div>
